# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtualenv
env/
venv/
.env
.venv

# Streamlit cache
.streamlit/

# Data / large binaries
data/hetionet/*.gz
data/hetionet/edges.tsv

# ML model (binary - committed separately if needed)
# ml_scoring_model.pkl

# Logs and temp
logs/
*.log
cache/
outputs/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
.venv312/
.flasklog.txt
.flaskout.txt
biocypher-log/
.claude/

# Docking — provisioned locally, not committed (see vendor/vina_bin/setup.sh)
# .qc/ = micromamba root (qc + drugdisc-agent envs)
.qc/
# vendor/vina_bin/*.exe = AutoDock Vina Windows binaries (re-downloadable)
vendor/vina_bin/*.exe
vendor/AtomisticSkills/**/__pycache__/

# Predictor DWPC cache — rebuilt from the DB on first use (services/repurposing_predictor.py)
data/dwpc_cache.npz

# Raw MONDO ontology download (83MB) — re-fetched by build_disease_table.py.
# The built disease_value.db (11MB) IS committed so the feature works out of the box.
data/disease_reference/mondo-simple.json

# CREEDS raw signatures (34MB, re-downloadable) — the compact signature_store.pkl is committed
data/signatures/creeds_drugs.json
data/signatures/creeds_diseases.json

# DRKG modern-KG: raw dumps + build intermediates (regenerate via services/drkg_train)
data/drkg/drkg.tsv
data/drkg/drkg.tar.gz
data/drkg/entity2src.tsv
data/drkg/relation_glossary.tsv
data/drkg/drkg_graph.npz
data/drkg/unichem_chembl_drugbank.txt.gz
data/drkg/embed/
data/drkg/._*

# ── Added 2026-07-22 (checkpoint hygiene) ────────────────────────────────
# Conda / QC environment (gigabytes of packages — never versioned)
.qc/
# pytest cache
.pytest_cache/
# temp / backup / scratch artifacts
*.tmp
*.bak
scratch_*.json
data/reverse_cache.*.tmp
# large regenerable data / model binaries (kept local, not in git)
data/kg_embeddings.npz
data/pos_model.pkl
data/external/
data/orange_book/
# accidental path-named output dirs (misconfigured writer)
C:Users*/

# PrimeKG raw dump + built artifacts (large; rebuild via services/build_primekg.py)
data/primekg/*.csv
data/primekg/*.tab
data/primekg/*.tsv
data/primekg/*.json
data/primekg/*.txt
data/primekg/*.log
data/primekg/*.npz
data/primekg/*.pkl
data/primekg/*.pt

# P1 runtime secrets and records — never versioned
data/audit/
data/auth/
