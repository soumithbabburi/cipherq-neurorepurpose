# RepurposeIQ / CipherQ — runtime dependencies
# Curated to what the Flask app actually imports. Version floors track the
# .venv312 build that the app is verified against (see run.bat).
#
# NOTE: install into the Python 3.12 venv (py -3.12 -m venv .venv312); RDKit's
# wheels are blocked by Windows Smart App Control on 3.14.

# ── Web framework ─────────────────────────────────────────────────────────────
Flask>=3.1

# ── Data / numerics ───────────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.11          # PBPK ODE integration (services/pbpk_simulation.py)

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9

# ── HTTP (ChEMBL / Open Targets / PubChem / NLM) ──────────────────────────────
requests>=2.31

# ── Cheminformatics & structure ───────────────────────────────────────────────
rdkit>=2023.3        # SMILES parsing, conformers, docking, descriptors
py3Dmol>=2.0         # 3-D molecule rendering
biopython>=1.79      # mmCIF → PDB conversion (real_pdb_fetcher.py)

# ── Knowledge graph ───────────────────────────────────────────────────────────
biocypher>=0.5.5     # services/class_comparison.py

# ── Probability-of-Success calibrator (Asset Scoring) ─────────────────────────
# Optional but real: services/train_pos_model.py fits the calibrator from ChEMBL
# outcomes → data/pos_model.pkl, loaded by services/pos_model.py. The analytic
# phase-transition model works without these, so they are soft dependencies.
scikit-learn>=1.3    # GradientBoosting + isotonic calibration
joblib>=1.3          # model (de)serialisation

# Removed (not imported anywhere in the codebase):
#   dash, dash-cytoscape, dash-bootstrap-components, streamlit, plotly  — abandoned Dash migration
#   lightgbm, xgboost  — not used (PoS uses scikit-learn GradientBoosting)
#   python-dotenv  — config.py uses its own .env parser
#   SQLAlchemy, networkx  — only pulled transitively, not imported directly
